/*
  Para adicionar comentários de documentação (que são exibidos quando o usuário passa o mouse por cima da função)
  É necessário adicionar /** antes do comentário e adicionar os parâmetros e retorno da função.
  É muito útil para especificar melhor o que uma função faz principalmente para trabalhar em conjunto com outras pessoas
*/

/**
 * 
 * @param {String} email user email. 
 * @param {String} password more than 6 characters.
 * @returns {number} user id.
 */
function signIn(email, password) {
  // Fluxo de autenticação do usuário

  return 7
}